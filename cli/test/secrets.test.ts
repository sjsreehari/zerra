import { describe,expect,it } from "vitest"; import { mkdtemp,readFile } from "node:fs/promises"; import { tmpdir } from "node:os"; import { join } from "node:path"; import { saveSecret,loadEncryptedSecret } from "../src/secrets.js";
describe("encrypted fallback",()=>{it("does not write a plaintext credential",async()=>{const f=join(await mkdtemp(join(tmpdir(),"zerra-")),"secret.json"); await saveSecret("x","very-secret",f,"passphrase"); const contents=await readFile(f,"utf8").catch(()=>""); if(contents) { expect(contents).not.toContain("very-secret"); expect(await loadEncryptedSecret(f,"passphrase")).toBe("very-secret"); }});});
