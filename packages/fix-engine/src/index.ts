import { mkdtemp, rm, writeFile, readFile } from "node:fs/promises"; import { tmpdir } from "node:os"; import { join } from "node:path"; import { runTool } from "@zerra/scanner"; import type { NormalizedFinding } from "@zerra/schema";
export function isUnifiedDiff(diff: string): boolean { return diff.startsWith("diff --git ") && /^--- a\/.+\n\+\+\+ b\/.+/m.test(diff) && diff.includes("@@"); }
export async function validatePatch(repo: string, diff: string) { if (!isUnifiedDiff(diff)) return { ok:false, reason:"Response was not a unified diff" }; const patch = join(await mkdtemp(join(tmpdir(), "zerra-patch-")), "fix.patch"); try { await writeFile(patch, diff, { mode: 0o600 }); const result = await runTool("git", ["apply", "--check", patch], repo); return result.code === 0 ? {ok:true} : {ok:false, reason:"git apply --check failed"}; } finally { await rm(patch, {force:true}).catch(()=>{}); } }
export function fixPrompt(finding: NormalizedFinding, context: string) { return `You are fixing one security finding. Return ONLY a minimal valid unified git diff. Never replace a whole file. Finding: ${JSON.stringify(finding)}\nContext:\n${context}`; }
export async function detectVerificationCommand(repo: string): Promise<string[] | undefined> { const { readFile, access } = await import("node:fs/promises"); try { const pkg=JSON.parse(await readFile(join(repo,"package.json"),"utf8")); if (pkg.scripts?.test) return ["npm","test","--","--runInBand"]; if (pkg.scripts?.lint) return ["npm","run","lint"]; } catch {} try { await access(join(repo,"Makefile")); return ["make","test"]; } catch {} return undefined; }
export async function applyAndVerify(repo:string,diff:string,rescan:(paths:string[])=>Promise<NormalizedFinding[]>) { const valid=await validatePatch(repo,diff); if(!valid.ok) return valid; const patch=join(await mkdtemp(join(tmpdir(),"zerra-apply-")),"fix.patch"); try { await writeFile(patch,diff,{mode:0o600}); const applied=await runTool("git",["apply","--whitespace=error",patch],repo); if(applied.code!==0) return {ok:false,reason:"Patch could not be applied"}; const paths=[...diff.matchAll(/^\+\+\+ b\/(.+)$/gm)].map(m=>m[1]); const command=await detectVerificationCommand(repo); if(command) { const test=await runTool(command[0],command.slice(1),repo,300_000,{npm_config_offline:"true"}); if(test.code!==0) return {ok:false,reason:"Repository verification command failed"}; } const findings=await rescan(paths); return findings.length===0?{ok:true,paths}:{ok:false,reason:"Re-scan found unresolved or new findings"}; } finally { await rm(patch,{force:true}).catch(()=>{}); } }
export async function generateAnthropicDiff(finding:NormalizedFinding,context:string) { const key=process.env.ANTHROPIC_API_KEY; if(!key) throw new Error("Anthropic fix generation is not enabled"); const response=await fetch("https://api.anthropic.com/v1/messages",{method:"POST",headers:{"x-api-key":key,"anthropic-version":"2023-06-01","content-type":"application/json"},body:JSON.stringify({model:process.env.ANTHROPIC_MODEL??"claude-sonnet-4-20250514",max_tokens:1800,messages:[{role:"user",content:fixPrompt(finding,context)}]})}); if(!response.ok) throw new Error(`Anthropic request failed (${response.status})`); const body=await response.json() as {content?:Array<{text?:string}>}; const diff=body.content?.map(c=>c.text??"").join("")??""; if(!isUnifiedDiff(diff)) throw new Error("Model returned an invalid patch"); return diff; }
