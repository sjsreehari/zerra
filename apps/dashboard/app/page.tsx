async function api(path:string) { const res=await fetch(`${process.env.API_URL??"http://localhost:3001"}${path}`,{cache:"no-store"}); return res.ok?res.json():[]; }
export default async function Page() { const repos=await api("/repos") as Array<{id:string;fullName:string;lastScan?:{status:string;startedAt:string}|null}>; return <main><h1>Repository posture</h1>{repos.length===0?<p>No installed repositories yet. Run <code>zerra init</code> to connect your GitHub App.</p>:<table><thead><tr><th>Repository</th><th>Latest scan</th></tr></thead><tbody>{repos.map(r=><tr key={r.id}><td>{r.fullName}</td><td>{r.lastScan?.status??"Awaiting first pull request"}</td></tr>)}</tbody></table>}</main>; }
